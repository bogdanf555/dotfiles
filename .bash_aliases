# create a directory and enter it
mk() {
  mkdir -pv $1; cd $1
}

# shortcut for ls to show in long listing
alias ll='ls -l'

# shorcut for ls to list also the hidden files
alias la='ls -a'

# shortcut for better ls
alias l='ls -la'

# shorcut for python
alias py=python3
